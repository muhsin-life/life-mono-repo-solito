import { TextInput as Input } from 'react-native'
import { styled } from 'nativewind'

export const TextInput = styled(Input, "focus-visible:outline-none focus-visible:ring-0 focus-visible:ring-offset-0")


